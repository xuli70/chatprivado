/**
 * Template de configuración para Chat Anónimo
 * Variables inyectadas desde Coolify en runtime usando envsubst
 * 
 * Para desarrollo local: copiar como config.js y agregar valores reales
 * Para producción: procesado automáticamente por Dockerfile
 */

// Configuración principal con variables de entorno
window.APP_CONFIG = {
    SUPABASE_URL: '${SUPABASE_URL}',
    SUPABASE_ANON_KEY: '${SUPABASE_ANON_KEY}',
    ADMIN_PASSWORD: '${ADMIN_PASSWORD}',
    AI_ACCESS_PASSWORD: '${AI_ACCESS_PASSWORD}',
    OPENAI_API_KEY: '${OPENAI_API_KEY}',
    AI_MODEL: '${AI_MODEL}'
};

// Compatibilidad con sistema legacy (window.env)
window.env = {
    SUPABASE_URL: '${SUPABASE_URL}',
    SUPABASE_ANON_KEY: '${SUPABASE_ANON_KEY}',
    ADMIN_PASSWORD: '${ADMIN_PASSWORD}',
    AI_ACCESS_PASSWORD: '${AI_ACCESS_PASSWORD}',
    OPENAI_API_KEY: '${OPENAI_API_KEY}',
    AI_MODEL: '${AI_MODEL}'
};

console.log('🚀 Configuración cargada desde variables de entorno (Coolify)');
console.log('📊 Variables configuradas:', {
    supabaseUrl: window.env.SUPABASE_URL ? '✅' : '❌',
    supabaseKey: window.env.SUPABASE_ANON_KEY ? '✅' : '❌',
    adminPassword: window.env.ADMIN_PASSWORD ? '✅' : '❌',
    aiAccessPassword: window.env.AI_ACCESS_PASSWORD ? '✅' : '❌',
    openaiKey: window.env.OPENAI_API_KEY ? '✅' : '❌',
    aiModel: window.env.AI_MODEL ? '✅' : '❌'
});